# Used only for the Capacitor Android build (npm run build:android).
# 10.0.2.2 is the Android emulator's alias for the host machine's localhost —
# swap this for your real deployed backend URL once it's live.
VITE_API_URL=http://10.0.2.2:4001/api
